.row
  .col-sm-2
    %h3= 'Modelli mezzi'
  .col-sm-2{ style: 'padding-top: 2.1em;'}
    ="Selezionati: #{@vehicle_models.size}" unless @vehicle_models.nil?
  .col-sm-3{ style: 'padding-top: 1.1em;'}
    -# =button_to 'Crea nuovo mezzo', new_vehicle_path, method: :get, style: "padding-top: 1.5em"
    = form_tag(:new_vehicle_model_search, method: :post, format: :js, remote: true) do |f|
      = hidden_field_tag :search, @search
      = submit_tag 'Crea nuovo modello', class: 'form-control popup-link', data: { name: 'edit_vehicle_model' }
      -# =submit_tag "Crea nuovo modello", new_vehicle_model_path, class: "btn btn-primary popup-link", remote: true

= form_tag(vehicle_models_path, method: :get) do |f|
  = text_field_tag :search, @search, placeholder: 'Cerca..', class: 'form-control autosearch autofocus', 'data-target': vehicle_models_path

#vehicle_models
  =render :partial => 'vehicle_models/list'

-# - unless @error.nil?
-#   .error
-#     =@error
-# :javascript

  // activatePopUp();
  // activateClose();
