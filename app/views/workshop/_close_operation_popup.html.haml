%h4= "Concludi operazione"
= form_tag( close_workshop_operation_path(@worksheet.number), remote: true, method: :post) do |f|
  = text_field_tag :notes, nil, placeholder: 'Note..', class: 'form-control', id: 'focus_element'
  = hidden_field_tag :protocol, protocol
  = submit_tag :ok
