= form_for @equipment_group do |f|
  - if @equipment_group.errors.any?
    #error_explanation
      %h2= "#{pluralize(@equipment_group.errors.count, "error")} prohibited this equipment_group from being saved:"
      %ul
        - @equipment_group.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
