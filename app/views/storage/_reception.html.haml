#reception_content
  =link_to "Genera ordine da DDT", items_new_order_path, :remote=> true, class: "btn btn-primary"
  -# -Order.all.each do |o|
  -#   =link_to "Ordine esistente", items_from_order_path(o.id), :remote=> true, class: "btn btn-primary"
