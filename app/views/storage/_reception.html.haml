#reception_content{ style: 'height: 100vh; padding-bottom: 22em;' }
  .row
    .col-sm-2
      =link_to "Inserimento libero", items_storage_insert_path, :remote=> true, class: "btn btn-primary"
    .col-sm-2
      =link_to "Inserimento da mezzo", items_vehicle_insert_path, :remote=> true, class: "btn btn-primary"
    .col-sm-2
      =link_to "Inserimento da DDT", items_new_order_path, :remote=> true, class: "btn btn-primary"
  -# =link_to "Scansione DDT", items_scan_order_path, :remote=> true, class: "btn btn-primary"
  -# -Order.all.each do |o|
  -#   =link_to "Ordine esistente", items_from_order_path(o.id), :remote=> true, class: "btn btn-primary"
