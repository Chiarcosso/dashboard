-# %p#notice= notice
%h3 Mezzi
.container
  .row
    .col-sm-1
      %b='Targa'
    .col-sm-2
      %b='Tipo'
    .col-sm-3
      %b='Proprietà'
    .col-sm-1
      %b='Circola'
    .col-sm-4
      %b='Note'
    .col-sm-1
    .col-sm-1


.container.scrollable-panel
  -Vehicle.all.each do |v|
    .row
      .col-sm-1
        =v.plate
      .col-sm-2
        =v.model.name
      .col-sm-3
        =v.property.name
      .col-sm-1
        =v.dismissed ? 'No' : 'Si'
      .col-sm-3
        =v.notes
      .col-sm-1
        =button_to 'Modifica', edit_vehicle_path(v.id), class: 'inline', method: :get
      .col-sm-1
        =button_to 'x', vehicle_path(v.id), remote: true, method: :delete, class: 'inline'

.container
  =button_to 'Crea', new_vehicle_path, method: :get
