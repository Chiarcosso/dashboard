= form_for @article_category do |f|
  - if @article_category.errors.any?
    #error_explanation
      %h2= "#{pluralize(@article_category.errors.count, "error")} prohibited this article_category from being saved:"
      %ul
        - @article_category.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
