json.extract! item, :id, :article_id, :purchaseDate, :price, :price, :discount, :discount, :seral, :state, :notes, :expiringDate, :transportDocument_id, :created_at, :updated_at
json.url item_url(item, format: :json)